## 拼多多语音搜索自动化 — 完整实现文档

---

## 完整数据流总览（参考微信电话模式）

```
用户: "打开拼多多搜索鞋子"
         │
         ▼
┌──────────────────────────────────────────────────────────────┐
│  Flutter 端 (home_content.dart)                              │
│  录音 → POST /api/chat/parse-audio → 收到响应              │
│  {action: "PINDUODUO_SEARCH", params: {keyword: "鞋子"}}  │
│  → VoiceIntentHandler.handle(action, params)                │
└──────────────────────────┬───────────────────────────────────┘
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Flutter 端 (voice_intent_handler.dart)                     │
│  _handlers["PINDUODUO_SEARCH"] = _openPinduoduoSearch       │
│  → _pinduoduoChannel.invokeMethod("startPinduoduoSearch",   │
│      {keyword: "鞋子"})                                       │
└──────────────────────────┬───────────────────────────────────┘
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Android 端 (MainActivity.java)                              │
│  _pinduoduoChannel handler                                   │
│  → 写入 SharedPreferences pinduoduo_keyword="鞋子"          │
│  → 拉起拼多多 app                                            │
│  → PinduoduoAutoService 状态机自动化输入搜索词                │
└──────────────────────────────────────────────────────────────┘
```

---

## 第一层：后端 LangChain4j

### 1.1 修改 ChatAssistant.java

改动点：修改 `@SystemMessage` 的内容，新增拼多多的识别指令。

修改后的 `@SystemMessage` 如下（其余部分保持不变）：

```java
@SystemMessage(
    "你是一个老人手机助手。用户输入可能包含方言口音导致的错别字（如'想吃拼过'指'苹果'）。\n" +
    "请纠错并提取意图，如果用户没有打开软件的意图只能回复问题本身，不要有与问题无关的话语，" +
    "如果用户有打开软件的意图则严格按照JSON格式返回：{\"action\": \"...\", \"params\":{...}, \"voiceFeedback\": \"...\"}。\n" +
    "当用户表达打开某个APP（如'打开微信/支付宝/抖音/美团'），必须返回 {\"action\": \"OPEN_APP\", \"params\": {\"appName\": \"微信\"}, \"voiceFeedback\": \"正在为你打开微信\"}，appName 必须是中文名称且和桌面显示一致。\n" +
    "示例1：用户说'我要买西兰花'，返回 {\"action\": \"MEITUAN_SEARCH\", \"params\": {\"keyword\": \"西兰花\"}, \"voiceFeedback\": \"正在为你打开美团购买\"}。\n" +
    // --- 新增：拼多多相关指令 ---
    "【拼多多相关】" +
    "当用户说'打开拼多多搜索XX'或'帮我搜一下拼多多XX'或'拼多多帮我找XX'时（带了搜索词），必须返回 " +
    "{\"action\": \"PINDUODUO_SEARCH\", \"params\": {\"keyword\": \"XX\"}, \"voiceFeedback\": \"正在为你打开拼多多搜索XX\"}。" +
    "当用户说'打开拼多多'（不带搜索词）时，返回 {\"action\": \"PINDUODU\", \"params\": {}, \"voiceFeedback\": \"正在为你打开拼多多\"}。" +
    "【严格指令约束】你必须且只能从以下合法列表中选择一个值作为 JSON 的 action 字段：{{actions}}"
)
```

### 1.2 修改 ActionEnum.java

在枚举中添加 `PINDUODU` 和 `PINDUODUO_SEARCH`（如果尚未存在）：

```java
package com.oldfull.enums;

public enum ActionEnum {
    CALL("打电话"),
    WECHAT("发微信"),
    WECHAT_VOICE_CALL("微信语音通话"),
    WECHAT_SCAN("微信扫一扫"),
    ALIPAY_SCAN("支付宝扫一扫"),
    ALIPAY_PAY("支付宝付款码"),
    MEITUAN_SEARCH("美团搜索"),
    MEITUAN("打开美团"),
    PINDUODU("打开拼多多"),           // ← 新增
    PINDUODUO_SEARCH("拼多多搜索"),    // ← 新增
    OPEN_APP("打开应用"),
    CAMERA("拍照"),
    UNKNOWN("未知操作");

    private final String description;

    ActionEnum(String description) {
        this.description = description;
    }

    public String getDescription() {
        return description;
    }

    public static String getEnums() {
        StringBuilder result = new StringBuilder();
        for (ActionEnum actionEnum : ActionEnum.values()) {
            if (actionEnum != UNKNOWN) {
                result.append(actionEnum.name()).append(",");
            }
        }
        return result.substring(0, Math.max(0, result.length() - 1));
    }
}
```

### 1.3 不需要修改的文件

- `AiIntentResponse.java` — `Map<String, String> params` 自然支持 `keyword` 字段
- `AiController.java` — `PINDUODUO_SEARCH` 不是 `OPEN_APP`，直接透传给前端，无需任何改动

---

## 第二层：Flutter 前端

### 2.1 修改 voice_intent_handler.dart

**改动 1**：新增 MethodChannel（在文件顶部 `_accessibilityChannel` 声明附近添加）：

```dart
static const MethodChannel _accessibilityChannel =
    MethodChannel('com.yourcompany.phone_java/accessibility');
// 新增
static const MethodChannel _pinduoduoChannel =
    MethodChannel('com.yourcompany.phone_java/pinduoduo');
```

**改动 2**：在 `_handlers` Map 中新增两个处理器：

```dart
static final Map<String, Future<void> Function(Map<String, dynamic>)> _handlers = {
  'MEITUAN_SEARCH': _openMeituanSearch,
  'MEITUAN': _openMeituanHome,
  'WECHAT': _openWeChat,
  'WECHAT_SCAN': _openWeChatScan,
  'WECHAT_VOICE_CALL': _startWeChatVoiceCall,
  'OPEN_APP': _openAppByName,
  'SEARCH_IN_APP': _searchInApp,
  // 新增
  'PINDUODUO_SEARCH': _openPinduoduoSearch,
  'PINDUODU': _openPinduoduoHome,
};
```

**改动 3**：在文件末尾（`_launchAnyApp` 函数之后）新增两个处理函数：

```dart
// 打开拼多多 + 无障碍自动化搜索
static Future<void> _openPinduoduoSearch(Map<String, dynamic> params) async {
  final keyword = (params['keyword'] ?? '').toString().trim();
  if (keyword.isEmpty) return;

  print('🔍 PINDUODUO_SEARCH 触发，关键词: $keyword');

  try {
    // 走原生端无障碍自动化
    await _pinduoduoChannel.invokeMethod('startPinduoduoSearch', {'keyword': keyword});
  } catch (e) {
    print('拼多多自动化失败: $e');
  }
}

// 打开拼多多主页（不带搜索词）
static Future<void> _openPinduoduoHome(Map<String, dynamic> params) async {
  print('🛒 PINDUODU 触发，打开拼多多主页');
  await _launchAnyApp(['pinduoduo://']);
}
```

### 2.2 不需要修改的文件

- `home_content.dart` — 第 143 行 `VoiceIntentHandler.handle(action, params)` 已通用处理所有 action，无需改动

---

## 第三层：Android 原生端

### 3.1 新建 PinduoduoAutoService.java

文件路径：`android/app/src/main/java/com/example/phone_java/PinduoduoAutoService.java`

```java
package com.example.phone_java;

import android.accessibilityservice.AccessibilityService;
import android.accessibilityservice.AccessibilityServiceInfo;
import android.content.Context;
import android.content.SharedPreferences;
import android.os.Bundle;
import android.os.Handler;
import android.os.Looper;
import android.util.Log;
import android.view.accessibility.AccessibilityEvent;
import android.view.accessibility.AccessibilityNodeInfo;

import java.util.ArrayDeque;
import java.util.ArrayList;
import java.util.Deque;
import java.util.List;

public class PinduoduoAutoService extends AccessibilityService {

    private static final String TAG = "PinduoduoAuto";
    private static final String PINDUODUO_PACKAGE = "com.xunmeng.pinduoduo";

    // ⚠️ 以下 ID 均为推测值，必须用 uiautomatorviewer 在真机上抓取确认后替换
    // 抓取方法：连接手机 → Android Studio → Tools → Layout Inspector
    //         或命令行执行：uiautomatorviewer.bat（位于 SDK/tools/bin/）
    private static final String ID_SEARCH_BAR = "com.xunmeng.pinduoduo:id/search_et";      // 搜索输入框
    private static final String ID_SEARCH_BTN = "com.xunmeng.pinduoduo:id/search_btn";     // 搜索按钮
    private static final String ID_HOME_SEARCH = "com.xunmeng.pinduoduo:id/ll_search";      // 首页搜索入口

    // 状态机常量
    private static final int STEP_IDLE              = 0;
    private static final int STEP_CLICK_SEARCH      = 1;   // 点击搜索框
    private static final int STEP_INPUT_KEYWORD     = 2;   // 输入关键词
    private static final int STEP_CLICK_SEARCH_BTN = 3;   // 点击搜索按钮
    private static final int STEP_WAIT_RESULT       = 4;   // 等待商品列表加载

    private final Handler handler = new Handler(Looper.getMainLooper());
    private int step = STEP_IDLE;
    private String targetKeyword = "";
    private long lastHandledRequestId = -1L;
    private long stepStartTime = 0L;

    @Override
    protected void onServiceConnected() {
        super.onServiceConnected();
        AccessibilityServiceInfo info = new AccessibilityServiceInfo();
        info.eventTypes = AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED
                | AccessibilityEvent.TYPE_WINDOW_CONTENT_CHANGED;
        info.feedbackType = AccessibilityServiceInfo.FEEDBACK_GENERIC;
        info.notificationTimeout = 100;
        info.packageNames = new String[]{PINDUODUO_PACKAGE};
        info.flags = AccessibilityServiceInfo.FLAG_REPORT_VIEW_IDS
                | AccessibilityServiceInfo.FLAG_RETRIEVE_INTERACTIVE_WINDOWS;
        setServiceInfo(info);
    }

    @Override
    public void onAccessibilityEvent(AccessibilityEvent event) {
        Log.d(TAG, ">>> onAccessibilityEvent 触发: " + event);

        if (event == null || event.getPackageName() == null) return;
        if (!PINDUODUO_PACKAGE.contentEquals(event.getPackageName())) return;

        long pendingRequestId = getPendingRequestId();
        String pendingKeyword = getPendingKeyword();

        Log.d(TAG, "SharedPreferences 状态:");
        Log.d(TAG, "   - pendingRequestId: " + pendingRequestId);
        Log.d(TAG, "   - pendingKeyword: [" + pendingKeyword + "]");
        Log.d(TAG, "   - lastHandledRequestId: " + lastHandledRequestId);
        Log.d(TAG, "   - 当前 step: " + step);

        // 检测到新任务，启动状态机
        if (pendingRequestId > 0 && pendingRequestId != lastHandledRequestId) {
            if (!pendingKeyword.isEmpty()) {
                targetKeyword = pendingKeyword;
                step = STEP_CLICK_SEARCH;
                lastHandledRequestId = pendingRequestId;
                stepStartTime = System.currentTimeMillis();
                Log.d(TAG, "========== 🎯 检测到新任务：搜索 " + targetKeyword + " ==========");
            }
        }

        if (step == STEP_IDLE) return;

        // 超时重置（15秒）
        if (System.currentTimeMillis() - stepStartTime > 15000) {
            Log.e(TAG, "步骤超时卡死，强制重置状态机！当前停留在 step=" + step);
            step = STEP_IDLE;
            clearPendingTask();
            return;
        }

        handler.removeCallbacks(stateMachineRunnable);
        handler.postDelayed(stateMachineRunnable, 500);
    }

    private final Runnable stateMachineRunnable = new Runnable() {
        @Override
        public void run() {
            AccessibilityNodeInfo root = getRootInActiveWindow();
            if (root == null) return;

            boolean success = false;

            switch (step) {
                case STEP_CLICK_SEARCH:
                    if (clickSearchEntry(root)) {
                        Log.d(TAG, "【1】成功点击搜索框");
                        step = STEP_INPUT_KEYWORD;
                        stepStartTime = System.currentTimeMillis();
                        success = true;
                        // 搜索框点击后需要等输入法弹出
                        handler.postDelayed(this, 800);
                        return;
                    }
                    break;

                case STEP_INPUT_KEYWORD:
                    if (inputKeyword(root, targetKeyword)) {
                        Log.d(TAG, "【2】成功在搜索框填入关键词：" + targetKeyword);
                        step = STEP_CLICK_SEARCH_BTN;
                        stepStartTime = System.currentTimeMillis();
                        success = true;
                        // 输入后稍微等一下再点搜索
                        handler.postDelayed(this, 500);
                        return;
                    }
                    break;

                case STEP_CLICK_SEARCH_BTN:
                    if (clickSearchButton(root) || clickSearchButtonByText(root)) {
                        Log.d(TAG, "【3】成功点击搜索按钮，进入商品列表");
                        step = STEP_WAIT_RESULT;
                        stepStartTime = System.currentTimeMillis();
                        success = true;
                    }
                    break;

                case STEP_WAIT_RESULT:
                    if (waitForSearchResult(root)) {
                        Log.d(TAG, "【4】商品列表已加载，任务完成！");
                        step = STEP_IDLE;
                        clearPendingTask();
                        success = true;
                    }
                    break;
            }

            if (!success && step != STEP_IDLE) {
                handler.removeCallbacks(this);
                handler.postDelayed(this, 500);
            }
        }
    };

    @Override
    public void onInterrupt() {}

    // ---------------------------------
    //         SharedPreferences
    // ---------------------------------
    private String getPendingKeyword() {
        SharedPreferences prefs = getSharedPreferences("com.example.phone_java", Context.MODE_PRIVATE);
        return prefs.getString("pinduoduo_keyword", "").trim();
    }

    private long getPendingRequestId() {
        SharedPreferences prefs = getSharedPreferences("com.example.phone_java", Context.MODE_PRIVATE);
        return prefs.getLong("pinduoduo_request_id", -1L);
    }

    private void clearPendingTask() {
        SharedPreferences prefs = getSharedPreferences("com.example.phone_java", Context.MODE_PRIVATE);
        prefs.edit()
                .remove("pinduoduo_keyword")
                .remove("pinduoduo_request_id")
                .apply();
    }

    // ---------------------------------
    //         各步骤具体实现
    // ---------------------------------

    /**
     * 点击搜索入口
     * 策略1：用 ID 找首页搜索框（精确匹配，最可靠）
     * 策略2：用文字找"搜索"节点
     * 策略3：用 contentDescription 找"搜索"
     */
    private boolean clickSearchEntry(AccessibilityNodeInfo root) {
        if (clickByViewId(root, ID_HOME_SEARCH)) return true;
        if (clickByViewId(root, ID_SEARCH_BAR)) return true;
        if (clickByAnyText(root, "搜索")) return true;
        if (clickByAnyDesc(root, "搜索")) return true;
        return false;
    }

    /**
     * 在搜索框输入关键词
     */
    private boolean inputKeyword(AccessibilityNodeInfo root, String keyword) {
        AccessibilityNodeInfo input = findFirstNodeByViewId(root, ID_SEARCH_BAR);
        if (input == null) {
            input = findFirstNodeByClass(root, "android.widget.EditText");
        }
        if (input == null) return false;

        Bundle args = new Bundle();
        args.putCharSequence(AccessibilityNodeInfo.ACTION_ARGUMENT_SET_TEXT_CHARSEQUENCE, keyword);
        return input.performAction(AccessibilityNodeInfo.ACTION_SET_TEXT, args);
    }

    /**
     * 点击搜索按钮（ID 精确匹配）
     */
    private boolean clickSearchButton(AccessibilityNodeInfo root) {
        return clickByViewId(root, ID_SEARCH_BTN);
    }

    /**
     * 点击搜索按钮（文字匹配兜底）
     * 拼多多搜索按钮文字可能是"搜索"、"搜索商品"等
     */
    private boolean clickSearchButtonByText(AccessibilityNodeInfo root) {
        if (clickByAnyText(root, "搜索")) return true;
        if (clickByAnyText(root, "搜索商品")) return true;
        return false;
    }

    /**
     * 等待商品列表出现
     * 策略：检测到商品相关特征即认为加载完成
     *      - 出现"¥"价格符号，或
     *      - 出现"拼"字（拼多多商品标题特征），或
     *      - 检测到商品卡片节点
     */
    private boolean waitForSearchResult(AccessibilityNodeInfo root) {
        // 检测到价格符号，说明商品列表已加载
        if (nodeExistsByText(root, "¥")) return true;
        // 检测到"拼"字（拼多多商品标题）
        if (nodeExistsByText(root, "拼")) return true;
        return false;
    }

    private boolean nodeExistsByText(AccessibilityNodeInfo root, String text) {
        List<AccessibilityNodeInfo> nodes = root.findAccessibilityNodeInfosByText(text);
        return nodes != null && !nodes.isEmpty();
    }

    // ---------------------------------
    //         底层通用工具方法
    // ---------------------------------
    private boolean clickByAnyText(AccessibilityNodeInfo root, String... texts) {
        List<AccessibilityNodeInfo> nodes = findNodesByAnyText(root, texts);
        for (AccessibilityNodeInfo node : nodes) {
            if (performClick(node)) return true;
        }
        return false;
    }

    private boolean clickByAnyDesc(AccessibilityNodeInfo root, String... descs) {
        List<AccessibilityNodeInfo> all = flatten(root);
        for (AccessibilityNodeInfo node : all) {
            CharSequence cd = node.getContentDescription();
            if (cd == null) continue;
            String value = cd.toString();
            for (String d : descs) {
                if (d != null && !d.isEmpty() && value.contains(d)) {
                    if (performClick(node)) return true;
                }
            }
        }
        return false;
    }

    private boolean clickByViewId(AccessibilityNodeInfo root, String viewId) {
        if (viewId == null || viewId.isEmpty()) return false;
        List<AccessibilityNodeInfo> nodes = root.findAccessibilityNodeInfosByViewId(viewId);
        if (nodes == null || nodes.isEmpty()) return false;
        for (AccessibilityNodeInfo node : nodes) {
            if (performClick(node)) return true;
        }
        return false;
    }

    private boolean performClick(AccessibilityNodeInfo node) {
        AccessibilityNodeInfo cur = node;
        while (cur != null) {
            if (cur.isClickable()) {
                return cur.performAction(AccessibilityNodeInfo.ACTION_CLICK);
            }
            cur = cur.getParent();
        }
        return false;
    }

    private List<AccessibilityNodeInfo> findNodesByAnyText(AccessibilityNodeInfo root, String... texts) {
        List<AccessibilityNodeInfo> result = new ArrayList<>();
        if (root == null) return result;
        for (String text : texts) {
            if (text == null || text.trim().isEmpty()) continue;
            List<AccessibilityNodeInfo> found = root.findAccessibilityNodeInfosByText(text);
            if (found != null && !found.isEmpty()) result.addAll(found);
        }
        return result;
    }

    private AccessibilityNodeInfo findFirstNodeByViewId(AccessibilityNodeInfo root, String viewId) {
        if (viewId == null || viewId.isEmpty()) return null;
        List<AccessibilityNodeInfo> nodes = root.findAccessibilityNodeInfosByViewId(viewId);
        return (nodes != null && !nodes.isEmpty()) ? nodes.get(0) : null;
    }

    private AccessibilityNodeInfo findFirstNodeByClass(AccessibilityNodeInfo root, String className) {
        if (root == null) return null;
        Deque<AccessibilityNodeInfo> queue = new ArrayDeque<>();
        queue.add(root);
        while (!queue.isEmpty()) {
            AccessibilityNodeInfo node = queue.poll();
            if (node == null) continue;
            CharSequence cls = node.getClassName();
            if (cls != null && className.contentEquals(cls)) return node;
            for (int i = 0; i < node.getChildCount(); i++) {
                AccessibilityNodeInfo child = node.getChild(i);
                if (child != null) queue.add(child);
            }
        }
        return null;
    }

    private List<AccessibilityNodeInfo> flatten(AccessibilityNodeInfo root) {
        List<AccessibilityNodeInfo> out = new ArrayList<>();
        if (root == null) return out;
        Deque<AccessibilityNodeInfo> queue = new ArrayDeque<>();
        queue.add(root);
        while (!queue.isEmpty()) {
            AccessibilityNodeInfo node = queue.poll();
            if (node == null) continue;
            out.add(node);
            for (int i = 0; i < node.getChildCount(); i++) {
                AccessibilityNodeInfo child = node.getChild(i);
                if (child != null) queue.add(child);
            }
        }
        return out;
    }
}
```

### 3.2 修改 MainActivity.java

**改动 1**：新增 `PINDUODUO_CHANNEL` 常量（在 `FLOAT_CHANNEL` 声明之后）：

```java
private static final String ACCESSIBILITY_CHANNEL = "com.yourcompany.phone_java/accessibility";
private static final String FLOAT_CHANNEL = "com.yourcompany.phone_java/float_window";
// 新增
private static final String PINDUODUO_CHANNEL = "com.yourcompany.phone_java/pinduoduo";
```

**改动 2**：新增 MethodChannel handler（在 `ACCESSIBILITY_CHANNEL` handler 之后，`FLOAT_CHANNEL` handler 之前添加）：

```java
new MethodChannel(flutterEngine.getDartExecutor().getBinaryMessenger(), ACCESSIBILITY_CHANNEL)
        .setMethodCallHandler((call, result) -> {
            if ("startWeChatCall".equals(call.method)) {
                String contact = call.argument("contact");
                boolean ok = cacheWeChatTargetAndLaunch(contact);
                result.success(ok);
            } else {
                result.notImplemented();
            }
        });

// 新增：拼多多搜索通道
new MethodChannel(flutterEngine.getDartExecutor().getBinaryMessenger(), PINDUODUO_CHANNEL)
        .setMethodCallHandler((call, result) -> {
            if ("startPinduoduoSearch".equals(call.method)) {
                String keyword = call.argument("keyword");
                boolean ok = cachePinduoduoKeywordAndLaunch(keyword);
                result.success(ok);
            } else {
                result.notImplemented();
            }
        });

new MethodChannel(flutterEngine.getDartExecutor().getBinaryMessenger(), FLOAT_CHANNEL)
        .setMethodCallHandler((call, result) -> {
            // ... FLOAT_CHANNEL 原有代码不变 ...
        });
```

**改动 3**：新增包名常量和方法（在 `WECHAT_PACKAGE` 常量之后添加）：

```java
private static final String TAG = "MainActivity";
private static final String WECHAT_PACKAGE = "com.tencent.mm";
// 新增
private static final String PINDUODUO_PACKAGE = "com.xunmeng.pinduoduo";

// 新增方法
private boolean cachePinduoduoKeywordAndLaunch(String keyword) {
    if (keyword == null || keyword.trim().isEmpty()) return false;

    SharedPreferences prefs = getSharedPreferences(PREF_NAME, Context.MODE_PRIVATE);
    prefs.edit()
            .putString("pinduoduo_keyword", keyword.trim())
            .putLong("pinduoduo_request_id", System.currentTimeMillis())
            .apply();

    Intent launchIntent = getPackageManager().getLaunchIntentForPackage(PINDUODUO_PACKAGE);
    if (launchIntent != null) {
        launchIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
        startActivity(launchIntent);
        Log.d(TAG, "✅ 拼多多已启动，关键词=" + keyword.trim());
        return true;
    }
    Log.e(TAG, "❌ 未找到拼多多包名: " + PINDUODUO_PACKAGE);
    return false;
}
```

### 3.3 修改 AndroidManifest.xml

在 `WeChatAutoService` 的 `<service>` 节点之后，新增 `PinduoduoAutoService` 的注册：

```xml
<service
    android:name=".WeChatAutoService"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>

<!-- 新增：拼多多无障碍服务 -->
<service
    android:name=".PinduoduoAutoService"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>
```

---

## 改动汇总表

| 文件 | 改动类型 |
|---|---|
| `backend/.../ChatAssistant.java` | 修改 `@SystemMessage`，新增拼多多识别规则 |
| `backend/.../ActionEnum.java` | 新增 `PINDUODU` 和 `PINDUODUO_SEARCH` 枚举值 |
| `backend/.../AiIntentResponse.java` | 不改 |
| `backend/.../AiController.java` | 不改 |
| `lib/utils/voice_intent_handler.dart` | 新增 `_pinduoduoChannel` + 2 个 handler |
| `lib/page/home_content.dart` | 不改 |
| `android/.../PinduoduoAutoService.java` | 新建文件，5步状态机 |
| `android/.../MainActivity.java` | 新增 `PINDUODUO_CHANNEL` handler + `cachePinduoduoKeywordAndLaunch()` |
| `android/.../AndroidManifest.xml` | 新增 `<service>` 注册 `PinduoduoAutoService` |

---

## 重要：节点 ID 抓取步骤

`PinduoduoAutoService.java` 中的以下常量是**推测值**，必须替换为真实值：

```java
private static final String ID_SEARCH_BAR   = "com.xunmeng.pinduoduo:id/search_et";
private static final String ID_SEARCH_BTN   = "com.xunmeng.pinduoduo:id/search_btn";
private static final String ID_HOME_SEARCH  = "com.xunmeng.pinduoduo:id/ll_search";
```

**抓取步骤：**

1. 手机连接电脑，开启 USB 调试
2. 打开拼多多 App，进入主页（不要停留在广告页），确保搜索框可见
3. 在 Android Studio 中：Tools → Layout Inspector → 选择拼多多进程
4. 找到搜索框和搜索按钮的节点，记录其 `resource-id`（格式为 `com.xunmeng.pinduoduo:id/xxx`）
5. 将 `PinduoduoAutoService.java` 中的 3 个常量替换为真实值
6. 重新编译安装 APK，在"无障碍"设置中启用"拼多多自动化"服务

---

---

# 微信发消息自动化（复用 WeChatAutoService）

> 复用已有的 `WeChatAutoService.java`，通过 `wechat_message` 字段区分"打电话"和"发消息"两条路径。打电话时该字段为空，发消息时非空，状态机在 STEP_CLICK_PLUS 之后自动路由到对应分支。

## 完整数据流

```
用户: "给儿子发微信说早点回家"
         │
         ▼
后端识别 → {action: "WECHAT_SEND_MESSAGE", params: {contact: "儿子", message: "早点回家"}}
         │
         ▼
Flutter VoiceIntentHandler → _sendWeChatMessage({contact, message})
         │
         ▼
Flutter MethodChannel → "startWeChatSendMessage" {contact, message}
         │
         ▼
Android MainActivity → 写入 SharedPreferences (contact + message + requestId)
         │
         ▼
Android WeChatAutoService → 检测到新任务
         │                        ↓
         │              pendingMessage 非空 → 发消息路径（3步）
         │              pendingMessage 为空 → 打电话路径（原有6步）
```

## 第一层：后端

### 1.1 修改 ActionEnum.java

在枚举中添加 `WECHAT_SEND_MESSAGE`：

```java
WECHAT("发微信"),
WECHAT_VOICE_CALL("微信语音通话"),
WECHAT_SCAN("微信扫一扫"),
WECHAT_SEND_MESSAGE("微信发消息"),   // ← 新增
```

### 1.2 修改 ChatAssistant.java 的 @SystemMessage

在 `@SystemMessage` 中新增发消息识别规则：

```
当用户说'给XX发微信说YY'或'发微信给XX告诉他YY'或'微信告诉XX，YY'时，必须返回
{"action": "WECHAT_SEND_MESSAGE", "params": {"contact": "XX", "message": "YY"}, "voiceFeedback": "好的，正在帮你发微信给XX"}。
```

### 1.3 AiController.java（不用改）

`params` 是通用 Map，`WECHAT_SEND_MESSAGE` 不是 `OPEN_APP`，所有参数直接透传给前端。

---

## 第二层：Flutter 前端

### 2.1 修改 voice_intent_handler.dart

**改动 1**：在 `_handlers` Map 中新增：

```dart
'WECHAT_SCAN': _openWeChatScan,
'WECHAT_VOICE_CALL': _startWeChatVoiceCall,
'WECHAT_SEND_MESSAGE': _sendWeChatMessage,  // ← 新增
'OPEN_APP': _openAppByName,
```

**改动 2**：在 `_startWeChatVoiceCall` 之后新增处理函数：

```dart
// 新增：发送微信消息
static Future<void> _sendWeChatMessage(Map<String, dynamic> params) async {
  final contact = (params['contact'] ?? params['contactName'] ?? '').toString().trim();
  final message = (params['message'] ?? '').toString().trim();
  if (contact.isEmpty || message.isEmpty) {
    print('❌ WECHAT_SEND_MESSAGE 缺少参数: contact=$contact, message=$message');
    return;
  }
  print('💬 WECHAT_SEND_MESSAGE 触发: 给 $contact 发 "$message"');
  try {
    await _accessibilityChannel.invokeMethod('startWeChatSendMessage', {
      'contact': contact,
      'message': message,
    });
  } catch (e) {
    print('微信发消息自动化失败: $e');
  }
}
```

---

## 第三层：Android 原生端

### 3.1 修改 MainActivity.java

**改动 1**：在 `ACCESSIBILITY_CHANNEL` handler 内部新增 `startWeChatSendMessage` 分支：

```java
new MethodChannel(flutterEngine.getDartExecutor().getBinaryMessenger(), ACCESSIBILITY_CHANNEL)
        .setMethodCallHandler((call, result) -> {
            if ("startWeChatCall".equals(call.method)) {
                String contact = call.argument("contact");
                boolean ok = cacheWeChatTargetAndLaunch(contact);
                result.success(ok);
            } else if ("startWeChatSendMessage".equals(call.method)) {
                // ← 新增：微信发消息
                String contact = call.argument("contact");
                String message = call.argument("message");
                boolean ok = cacheWeChatSendAndLaunch(contact, message);
                result.success(ok);
            } else {
                result.notImplemented();
            }
        });
```

**改动 2**：新增 `cacheWeChatSendAndLaunch` 方法：

```java
// 缓存微信发消息参数并拉起微信
private boolean cacheWeChatSendAndLaunch(String contact, String message) {
    if (contact == null || contact.trim().isEmpty() || message == null || message.trim().isEmpty()) {
        android.util.Log.e(TAG, "❌ contact 或 message 为空，拒绝执行");
        return false;
    }
    SharedPreferences prefs = getSharedPreferences(PREF_NAME, Context.MODE_PRIVATE);
    prefs.edit()
            .putString(KEY_WECHAT_CONTACT, contact.trim())
            .putString("wechat_message", message.trim())
            .putLong(KEY_WECHAT_REQUEST_ID, System.currentTimeMillis())
            .apply();
    android.util.Log.d(TAG, "✅ 微信发消息参数缓存: contact=" + contact.trim() + ", message=" + message.trim());
    Intent launchIntent = getPackageManager().getLaunchIntentForPackage(WECHAT_PACKAGE);
    if (launchIntent != null) {
        launchIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
        startActivity(launchIntent);
        android.util.Log.d(TAG, "✅ 微信已启动，等待无障碍服务自动化");
        return true;
    }
    android.util.Log.e(TAG, "❌ 未找到微信包名");
    return false;
}
```

### 3.2 修改 WeChatAutoService.java

**改动 1**：新增状态机常量（接在 STEP_CLICK_POPUP_VOICE 之后）：

```java
private static final int STEP_CLICK_POPUP_VOICE = 6;
// ← 新增
private static final int STEP_INPUT_MESSAGE = 7;   // 发消息：输入消息文字
private static final int STEP_CLICK_SEND    = 8;   // 发消息：点击发送按钮
```

**改动 2**：新增成员变量：

```java
private int step = STEP_IDLE;
private String targetContact = "";
private String targetMessage = "";  // ← 新增：待发送消息内容（打电话时为空）
```

**改动 3**：新增节点 ID 常量：

```java
// ⚠️ 以下 ID 为推测值，需用 uiautomatorviewer 在微信聊天页抓取确认
private static final String ID_SEND_BTN = "com.tencent.mm:id/a3k"; // 发送按钮
```

**改动 4**：扩展 `onAccessibilityEvent` 中的 SharedPreferences 读取和任务检测：

```java
long pendingRequestId = getPendingRequestId();
String pendingContact = getPendingContact();
String pendingMessage = getPendingMessage();  // ← 新增

// 检测到新任务时，同时读取 message
if (pendingRequestId > 0 && pendingRequestId != lastHandledRequestId) {
    if (!pendingContact.isEmpty()) {
        targetContact = pendingContact;
        targetMessage = pendingMessage;  // ← 新增
        step = STEP_OPEN_SEARCH;
        lastHandledRequestId = pendingRequestId;
        stepStartTime = System.currentTimeMillis();
        if (!targetMessage.isEmpty()) {
            Log.d(TAG, "========== 💬 检测到新任务：给 " + targetContact + " 发消息: " + targetMessage + " ==========");
        } else {
            Log.d(TAG, "========== 📞 检测到新任务：拨打给 " + targetContact + " ==========");
        }
    }
}
```

**改动 5**：修改 `STEP_CLICK_PLUS` 的分支逻辑：

```java
case STEP_CLICK_PLUS:
    if (clickMoreFunction(root)) {
        Log.d(TAG, "【4】成功点击聊天页右下角的加号");
        // 关键分支：targetMessage 非空 → 发消息；空 → 打电话
        if (!targetMessage.isEmpty()) {
            step = STEP_INPUT_MESSAGE;    // 发消息：直接进入输入
        } else {
            step = STEP_CLICK_PANEL_VIDEO; // 打电话：选视频→语音
        }
        stepStartTime = System.currentTimeMillis();
        success = true;
    }
    break;
```

**改动 6**：在 switch 语句末尾（STEP_CLICK_POPUP_VOICE 之后）新增发消息步骤：

```java
case STEP_INPUT_MESSAGE:
    if (inputMessageText(root, targetMessage)) {
        Log.d(TAG, "【5💬】成功在输入框填入消息：" + targetMessage);
        step = STEP_CLICK_SEND;
        stepStartTime = System.currentTimeMillis();
        success = true;
        handler.postDelayed(this, 500);
        return;
    }
    break;

case STEP_CLICK_SEND:
    if (clickSendButton(root)) {
        Log.d(TAG, "【6💬】成功点击发送按钮！消息已发送，任务完成！");
        step = STEP_IDLE;
        clearPendingTask();
        success = true;
    }
    break;
```

**改动 7**：新增 SharedPreferences 读取方法：

```java
private String getPendingMessage() {
    SharedPreferences prefs = getSharedPreferences("com.example.phone_java", Context.MODE_PRIVATE);
    return prefs.getString("wechat_message", "").trim();
}
```

**改动 8**：修改 `clearPendingTask`，清理 `wechat_message`：

```java
private void clearPendingTask() {
    SharedPreferences prefs = getSharedPreferences("com.example.phone_java", Context.MODE_PRIVATE);
    prefs.edit()
            .remove("wechat_contact")
            .remove("wechat_message")  // ← 新增
            .remove("wechat_request_id")
            .apply();
}
```

**改动 9**：在 `clickMoreFunction` 之后新增发消息专用方法：

```java
/**
 * 发消息专用：在聊天输入框填入消息文字
 * 优先按 ID_SEARCH_INPUT（d98）查找，找不到再用 EditText 兜底
 */
private boolean inputMessageText(AccessibilityNodeInfo root, String message) {
    AccessibilityNodeInfo input = findFirstNodeByViewId(root, ID_SEARCH_INPUT);
    if (input == null) {
        input = findFirstNodeByClass(root, "android.widget.EditText");
    }
    if (input == null) return false;
    Bundle args = new Bundle();
    args.putCharSequence(AccessibilityNodeInfo.ACTION_ARGUMENT_SET_TEXT_CHARSEQUENCE, message);
    return input.performAction(AccessibilityNodeInfo.ACTION_SET_TEXT, args);
}

/**
 * 发消息专用：点击发送按钮
 */
private boolean clickSendButton(AccessibilityNodeInfo root) {
    if (clickByViewId(root, ID_SEND_BTN)) return true;
    if (clickByAnyText(root, "发送")) return true;
    return false;
}
```

---

## 改动汇总表

| 文件 | 改动类型 |
|---|---|
| `backend/.../ActionEnum.java` | 新增 `WECHAT_SEND_MESSAGE` 枚举 |
| `backend/.../ChatAssistant.java` | @SystemMessage 新增发消息识别规则 |
| `backend/.../AiController.java` | 不改 |
| `lib/utils/voice_intent_handler.dart` | 新增 `_sendWeChatMessage` handler |
| `android/.../MainActivity.java` | 新增 `startWeChatSendMessage` 通道 + `cacheWeChatSendAndLaunch()` |
| `android/.../WeChatAutoService.java` | 扩展状态机，新增 STEP_INPUT_MESSAGE + STEP_CLICK_SEND，支持分支路由 |
| `android/.../AndroidManifest.xml` | 不改（复用已有 WeChatAutoService） |

## ID 抓取说明

- `ID_SEND_BTN = "com.tencent.mm:id/a3k"` 为推测值，需在微信聊天页面（光标在输入框内）用 uiautomatorviewer 抓取"发送"按钮的真实 `resource-id` 后替换。
- `ID_SEARCH_INPUT = "com.tencent.mm:id/d98"` 复用微信搜索框 ID，微信聊天输入框和搜索输入框共用同一控件，无需额外抓取；如失效可降级使用 `findFirstNodeByClass("android.widget.EditText")` 兜底。

---

# 高德地图导航自动化

> 用户说"导航去XX"或"带我去XX" → AI 识别目的地 → 无障碍服务自动启动高德地图、输入目的地、点击开始导航，全程无需手动操作。

## 完整数据流

```
用户: "导航去北京天安门"
         │
         ▼
后端识别 → {action: "AMAP_NAVIGATE", params: {destination: "北京天安门"}}
         │
         ▼
Flutter VoiceIntentHandler → _navigateWithAmap({destination: "北京天安门"})
         │
         ▼
Flutter MethodChannel → "startAmapNavi" {destination: "北京天安门"}
         │
         ▼
Android MainActivity → 写入 SharedPreferences (amap_destination + requestId)
         │                    │
         │                    ├── 有经纬度 → androidamap://navi?... 直接 URI 导航
         │                    └── 无经纬度 → 直接拉起高德地图主页
         │
         ▼
Android AmapAutoService → 6步状态机自动化搜索 + 点导航
         │
         │  状态机步骤:
         │  STEP_CLICK_SEARCH_BOX → 点击首页搜索框
         │  STEP_INPUT_DESTINATION → 输入目的地名称
         │  STEP_CLICK_SEARCH_BTN → 点击搜索按钮
         │  STEP_CLICK_RESULT_ITEM → 点击第一个搜索结果
         │  STEP_CLICK_GO_HERE → 点击"到这里去"
         │  STEP_CONFIRM_NAVI → 确认开始导航
```

---

## 第一层：后端 LangChain4j

### 1.1 修改 ActionEnum.java

在枚举中添加 `AMAP_NAVIGATE`：

```java
public enum ActionEnum {
    // ... 现有枚举 ...
    OPEN_APP("打开应用"),
    WECHAT_SEND_MESSAGE("微信发消息"),
    AMAP_NAVIGATE("高德地图导航"),   // ← 新增
    CAMERA("拍照"),
    UNKNOWN("未知操作");
}
```

### 1.2 修改 ChatAssistant.java 的 @SystemMessage

在 `@SystemMessage` 中新增高德导航识别规则：

```
"【高德地图导航】" +
"当用户说'导航去XX'或'带我去XX'或'帮我导航到XX'或'我想去XX'或'打开导航去XX'或'高德导航XX'或'用高德地图导航去XX'时（带了目的地），必须返回 " +
"{\"action\": \"AMAP_NAVIGATE\", \"params\": {\"destination\": \"XX\"}, \"voiceFeedback\": \"好的，正在为你打开高德地图导航去XX\"}。"
```

### 1.3 AiController.java（不用改）

`params` 是通用 Map，`AMAP_NAVIGATE` 不是 `OPEN_APP`，所有参数直接透传给前端。

---

## 第二层：Flutter 前端

### 2.1 修改 voice_intent_handler.dart

Flutter 端 `_navigateWithAmap` 方法已经存在，本次改动将其升级为优先调用原生无障碍通道，Deep Link 作为兜底：

```dart
static Future<void> _navigateWithAmap(Map<String, dynamic> params) async {
  final destination = (params['destination'] ?? params['addr'] ?? params['address'] ?? '').toString().trim();
  if (destination.isEmpty) return;

  // 方案1：优先通过原生无障碍服务（自动搜索+点击导航）
  try {
    final result = await _accessibilityChannel.invokeMethod<bool>('startAmapNavi', {
      'destination': destination,
      'latitude': lat,
      'longitude': lon,
    });
    if (result == true) return;
  } catch (e) {
    // 回退到 Deep Link
  }

  // 方案2：Deep Link 兜底（高德地图客户端 URI Scheme）
  final encodedName = Uri.encodeComponent(destination);
  // 有经纬度: amapuri://route/plan/?dlat=&dlon=&dname=&dev=0&t=0
  // 无经纬度: androidamap://openNavi?sourceApplication=appname&keyword=...
  // ...
}
```

---

## 第三层：Android 原生端

### 3.1 修改 MainActivity.java

**改动 1**：新增常量：

```java
public static final String KEY_AMAP_REQUEST_ID = "amap_request_id";
public static final String KEY_AMAP_DESTINATION = "amap_destination";
private static final String AMAP_PACKAGE = "com.autonavi.minimap";
```

**改动 2**：在 `ACCESSIBILITY_CHANNEL` handler 中新增 `startAmapNavi` 分支：

```java
} else if ("startAmapNavi".equals(call.method)) {
    String destination = call.argument("destination");
    boolean ok = cacheAmapNaviAndLaunch(destination);
    result.success(ok);
}
```

**改动 3**：新增 `cacheAmapNaviAndLaunch()` 方法：

```java
private boolean cacheAmapNaviAndLaunch(String destination) {
    SharedPreferences prefs = getSharedPreferences(PREF_NAME, Context.MODE_PRIVATE);
    prefs.edit()
            .putString(KEY_AMAP_DESTINATION, destination.trim())
            .putLong(KEY_AMAP_REQUEST_ID, System.currentTimeMillis())
            .apply();

    // 优先通过 URI Scheme 直接导航
    String navUri = "androidamap://navi?sourceApplication=appname&keyword="
            + Uri.encode(destination.trim());
    Intent navIntent = new Intent(Intent.ACTION_VIEW, Uri.parse(navUri));
    navIntent.setPackage(AMAP_PACKAGE);
    if (navIntent.resolveActivity(getPackageManager()) != null) {
        navIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
        startActivity(navIntent);
        return true;
    }

    // 兜底：拉起高德主页，由无障碍服务自动搜索
    Intent launchIntent = getPackageManager().getLaunchIntentForPackage(AMAP_PACKAGE);
    if (launchIntent != null) {
        launchIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
        startActivity(launchIntent);
        return true;
    }
    return false;
}
```

### 3.2 新建 AmapAutoService.java

文件路径：`android/app/src/main/java/com/example/phone_java/AmapAutoService.java`

完整的 6 步状态机无障碍服务：

| 步骤 | 状态常量 | 操作 | 节点 ID（推测值，需抓取替换） |
|---|---|---|---|
| 1 | `STEP_CLICK_SEARCH_BOX` | 点击首页搜索框 | `com.autonavi.minimap:id/search_box` |
| 2 | `STEP_INPUT_DESTINATION` | 输入目的地 | `com.autonavi.minimap:id/search_input` |
| 3 | `STEP_CLICK_SEARCH_BTN` | 点击搜索按钮 | `com.autonavi.minimap:id/search_btn` |
| 4 | `STEP_CLICK_RESULT_ITEM` | 点击第一个搜索结果 | `com.autonavi.minimap:id/result_item` |
| 5 | `STEP_CLICK_GO_HERE` | 点击"到这里去"/"导航" | `com.autonavi.minimap:id/go_here` |
| 6 | `STEP_CONFIRM_NAVI` | 确认开始导航 | `com.autonavi.minimap:id/start_navi` |

**关键特性：**
- 智能跳过搜索框：检测到从搜索结果页启动时自动跳过 STEP_CLICK_SEARCH_BOX
- 多种文字匹配兜底：搜索按钮可能是"搜索"/"查找"/"确定"
- 暴力点击父节点：参考 WeChatAutoService 的 `performClick` 模式，逐层向上找可点击父节点
- 超时保护：20秒内未完成则重置状态机

### 3.3 修改 AndroidManifest.xml

在 `WeChatAutoService` 之后新增 `AmapAutoService` 注册：

```xml
<service
    android:name=".AmapAutoService"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>
```

---

## 改动汇总表

| 文件 | 改动类型 |
|---|---|
| `backend/.../ActionEnum.java` | 新增 `AMAP_NAVIGATE` 枚举 |
| `backend/.../ChatAssistant.java` | @SystemMessage 新增高德导航识别规则 |
| `backend/.../AiController.java` | 不改 |
| `lib/utils/voice_intent_handler.dart` | `_navigateWithAmap` 优先调用原生无障碍通道 |
| `android/.../MainActivity.java` | 新增 `startAmapNavi` 通道 + `cacheAmapNaviAndLaunch()` |
| `android/.../AmapAutoService.java` | 新建文件，6步状态机 |
| `android/.../AndroidManifest.xml` | 新增 `<service>` 注册 `AmapAutoService` |

---

## 重要：节点 ID 抓取步骤

`AmapAutoService.java` 中的以下常量是**推测值**，必须替换为真实值：

```java
private static final String ID_HOME_SEARCH_BOX = "com.autonavi.minimap:id/search_box";
private static final String ID_HOME_SEARCH_ET  = "com.autonavi.minimap:id/search_et";
private static final String ID_SEARCH_INPUT     = "com.autonavi.minimap:id/search_input";
private static final String ID_SEARCH_BTN       = "com.autonavi.minimap:id/search_btn";
private static final String ID_RESULT_ITEM      = "com.autonavi.minimap:id/result_item";
private static final String ID_NAVIGATE_BTN    = "com.autonavi.minimap:id/navi_btn";
private static final String ID_GO_HERE          = "com.autonavi.minimap:id/go_here";
private static final String ID_START_NAVI       = "com.autonavi.minimap:id/start_navi";
```

**抓取步骤：**

1. 手机连接电脑，开启 USB 调试
2. 打开高德地图 App，进入主页（确保搜索框可见）
3. 在 Android Studio 中：Tools → Layout Inspector → 选择高德地图进程
4. 依次操作并抓取以下页面的节点 ID：
   - 首页：找到搜索框的 `resource-id`
   - 搜索页：找到搜索输入框和搜索按钮的 `resource-id`
   - 搜索结果页：找到列表项的 `resource-id`
   - POI 详情页：找到"到这里去"/"导航"按钮的 `resource-id`
5. 将 `AmapAutoService.java` 中的 8 个常量替换为真实值
---

# 美团搜索自动化

> 用户说"打开美团搜索XX"或"美团帮我找XX" → AI 识别关键词 → 无障碍服务自动点击搜索框、输入关键词、点搜索，全程无需手动操作。

## 完整数据流

```
用户: "美团搜索北京烤鸭"
         │
         ▼
后端识别 → {action: "MEITUAN_SEARCH", params: {keyword: "北京烤鸭"}}
         │
         ▼
Flutter VoiceIntentHandler → _openMeituanSearch({keyword: "北京烤鸭"})
         │
         ▼
Flutter MethodChannel → "startMeituanSearch" {keyword: "北京烤鸭"}
         │
         ▼
Android MainActivity → 写入 SharedPreferences + 拉起美团
         │
         ▼
Android MeituanAutoService → 4步状态机自动化搜索
         │
         │  状态机步骤:
         │  STEP_CLICK_SEARCH_BOX → 点击首页搜索框
         │  STEP_INPUT_KEYWORD → 输入搜索关键词
         │  STEP_CLICK_SEARCH_BTN → 点击搜索按钮
         │  STEP_WAIT_RESULT → 等待商品列表加载 ✅ 完成
```

---

## 第一层：Flutter 前端

### 1.1 修改 voice_intent_handler.dart

`_openMeituanSearch` 升级为优先调用原生无障碍通道，Deep Link 为兜底：

```dart
static Future<void> _openMeituanSearch(Map<String, dynamic> params) async {
  final keyword = (params['keyword'] ?? '').toString().trim();
  if (keyword.isEmpty) return;

  // 方案1：优先通过原生无障碍服务
  try {
    final result = await _accessibilityChannel.invokeMethod<bool>('startMeituanSearch', {
      'keyword': keyword,
    });
    if (result == true) return;
  } catch (e) {
    // 回退到 Deep Link
  }

  // 方案2：Deep Link 兜底
  final encoded = Uri.encodeComponent(keyword);
  await _launchAnyApp([
    'imeituan://www.meituan.com/search/result?keyword=$encoded',
    'imeituan://www.meituan.com/',
  ]);
}
```

---

## 第二层：Android 原生端

### 2.1 修改 MainActivity.java

**改动 1**：新增常量：

```java
public static final String KEY_MEITUAN_REQUEST_ID = "meituan_request_id";
public static final String KEY_MEITUAN_KEYWORD = "meituan_keyword";
private static final String MEITUAN_PACKAGE = "com.sankuai.meituan";
```

**改动 2**：在 `ACCESSIBILITY_CHANNEL` handler 中新增 `startMeituanSearch` 分支：

```java
} else if ("startMeituanSearch".equals(call.method)) {
    String keyword = call.argument("keyword");
    boolean ok = cacheMeituanKeywordAndLaunch(keyword);
    result.success(ok);
}
```

**改动 3**：新增 `cacheMeituanKeywordAndLaunch()` 方法：

```java
private boolean cacheMeituanKeywordAndLaunch(String keyword) {
    SharedPreferences prefs = getSharedPreferences(PREF_NAME, Context.MODE_PRIVATE);
    prefs.edit()
            .putString(KEY_MEITUAN_KEYWORD, keyword.trim())
            .putLong(KEY_MEITUAN_REQUEST_ID, System.currentTimeMillis())
            .apply();

    Intent launchIntent = getPackageManager().getLaunchIntentForPackage(MEITUAN_PACKAGE);
    if (launchIntent != null) {
        launchIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
        startActivity(launchIntent);
        return true;
    }
    return false;
}
```

### 2.2 新建 MeituanAutoService.java

文件路径：`android/app/src/main/java/com/example/phone_java/MeituanAutoService.java`

完整的 4 步状态机无障碍服务：

| 步骤 | 状态常量 | 操作 | 节点 ID（推测值，需抓取替换） |
|---|---|---|---|
| 1 | `STEP_CLICK_SEARCH_BOX` | 点击首页搜索框 | `com.sankuai.meituan:id/home_search_wrapper` |
| 2 | `STEP_INPUT_KEYWORD` | 输入搜索关键词 | `com.sankuai.meituan:id/search_et` |
| 3 | `STEP_CLICK_SEARCH_BTN` | 点击搜索按钮 | `com.sankuai.meituan:id/search_btn` |
| 4 | `STEP_WAIT_RESULT` | 等待商品列表加载 | 检测「¥」「找相似」「立即预订」等特征 |

**关键特性：**
- 启动时检测 Activity 类型，已在搜索页则直接跳过 STEP_CLICK_SEARCH_BOX
- 智能匹配搜索输入框：优先找带占位文案「搜索商家、商品、品类」的 EditText
- 20秒超时保护，自动重置状态机

### 2.3 修改 AndroidManifest.xml

在 `AmapAutoService` 之后新增 `MeituanAutoService` 注册：

```xml
<service
    android:name=".MeituanAutoService"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>
```

---

## 改动汇总表

| 文件 | 改动类型 |
|---|---|
| `lib/utils/voice_intent_handler.dart` | `_openMeituanSearch` 优先调用原生无障碍通道 |
| `android/.../MainActivity.java` | 新增 `startMeituanSearch` 通道 + `cacheMeituanKeywordAndLaunch()` |
| `android/.../MeituanAutoService.java` | 新建文件，4步状态机 |
| `android/.../AndroidManifest.xml` | 新增 `<service>` 注册 `MeituanAutoService` |

---

## 重要：节点 ID 抓取步骤

`MeituanAutoService.java` 中的以下常量是**推测值**，必须替换为真实值：

```java
private static final String ID_HOME_SEARCH_WRAPPER = "com.sankuai.meituan:id/home_search_wrapper";
private static final String ID_HOME_SEARCH_TXT     = "com.sankuai.meituan:id/home_search_txt";
private static final String ID_SEARCH_ET            = "com.sankuai.meituan:id/search_et";
private static final String ID_SEARCH_BTN           = "com.sankuai.meituan:id/search_btn";
```

**抓取步骤：**

1. 手机连接电脑，开启 USB 调试
2. 打开美团 App，进入首页（确保搜索框可见）
3. 在 Android Studio 中：Tools → Layout Inspector → 选择美团进程
4. 依次操作并抓取以下页面的节点 ID：
   - 首页：找到搜索框 wrapper 和内部 TextView 的 `resource-id`
   - 搜索页：找到搜索输入框和搜索按钮的 `resource-id`
5. 将 `MeituanAutoService.java` 中的 4 个常量替换为真实值
6. 重新编译安装 APK，在"无障碍"设置中启用"美团自动化"服务

---

# 拼多多搜索自动化

> 用户说"打开拼多多搜索XX"或"帮我搜一下拼多多XX" → AI 识别关键词 → 无障碍服务自动点击搜索框、输入关键词、点搜索，全程无需手动操作。

## 完整数据流

```
用户: "打开拼多多搜索耳机"
         │
         ▼
后端识别 → {action: "PINDUODUO_SEARCH", params: {keyword: "耳机"}}
         │
         ▼
Flutter VoiceIntentHandler → _openPinduoduoSearch({keyword: "耳机"})
         │
         ▼
Flutter MethodChannel → "startPinduoduoSearch" {keyword: "耳机"}
         │
         ▼
Android MainActivity → 写入 SharedPreferences + 拉起拼多多
         │
         ▼
Android PinduoduoAutoService → 4步状态机自动化搜索
         │
         │  状态机步骤:
         │  STEP_CLICK_SEARCH_ENTRY → 点击首页搜索入口
         │  STEP_INPUT_KEYWORD → 输入搜索关键词
         │  STEP_CLICK_SEARCH_BTN → 点击搜索按钮
         │  STEP_WAIT_RESULT → 等待商品列表加载 ✅ 完成
```

---

## 第一层：Flutter 前端

### 1.1 修改 voice_intent_handler.dart

在 `_handlers` Map 中新增两个处理器，并新增处理函数：

```dart
// 在 _handlers 中注册
'PINDUODUO_SEARCH': _openPinduoduoSearch,
'PINDUODU': _openPinduoduoHome,

// _openPinduoduoSearch：优先原生无障碍，兜底 Deep Link
static Future<void> _openPinduoduoSearch(Map<String, dynamic> params) async {
  final keyword = (params['keyword'] ?? '').toString().trim();
  if (keyword.isEmpty) return;

  // 方案1：原生无障碍通道
  try {
    final result = await _accessibilityChannel.invokeMethod<bool>('startPinduoduoSearch', {
      'keyword': keyword,
    });
    if (result == true) return;
  } catch (e) { /* 回退 */ }

  // 方案2：Deep Link 兜底
  final encoded = Uri.encodeComponent(keyword);
  await _launchAnyApp([
    'pinduoduo://com.xunmeng.pinduoduo/search?keyword=$encoded',
    'pinduoduo://',
  ]);
}

// _openPinduoduoHome：仅打开拼多多主页
static Future<void> _openPinduoduoHome(Map<String, dynamic> params) async {
  await _launchAnyApp(['pinduoduo://']);
}
```

---

## 第二层：Android 原生端

### 2.1 修改 MainActivity.java

**改动 1**：新增常量：

```java
public static final String KEY_PINDUODUO_REQUEST_ID = "pinduoduo_request_id";
public static final String KEY_PINDUODUO_KEYWORD = "pinduoduo_keyword";
private static final String PINDUODUO_PACKAGE = "com.xunmeng.pinduoduo";
```

**改动 2**：在 `ACCESSIBILITY_CHANNEL` handler 中新增 `startPinduoduoSearch` 分支：

```java
} else if ("startPinduoduoSearch".equals(call.method)) {
    String keyword = call.argument("keyword");
    boolean ok = cachePinduoduoKeywordAndLaunch(keyword);
    result.success(ok);
}
```

**改动 3**：新增 `cachePinduoduoKeywordAndLaunch()` 方法：

```java
private boolean cachePinduoduoKeywordAndLaunch(String keyword) {
    SharedPreferences prefs = getSharedPreferences(PREF_NAME, Context.MODE_PRIVATE);
    prefs.edit()
            .putString(KEY_PINDUODUO_KEYWORD, keyword.trim())
            .putLong(KEY_PINDUODUO_REQUEST_ID, System.currentTimeMillis())
            .apply();

    Intent launchIntent = getPackageManager().getLaunchIntentForPackage(PINDUODUO_PACKAGE);
    if (launchIntent != null) {
        launchIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
        startActivity(launchIntent);
        return true;
    }
    return false;
}
```

### 2.2 新建 PinduoduoAutoService.java

文件路径：`android/app/src/main/java/com/example/phone_java/PinduoduoAutoService.java`

完整的 4 步状态机无障碍服务：

| 步骤 | 状态常量 | 操作 | 节点 ID（推测值，需抓取替换） |
|---|---|---|---|
| 1 | `STEP_CLICK_SEARCH_ENTRY` | 点击首页搜索入口 | `com.xunmeng.pinduoduo:id/search_container` |
| 2 | `STEP_INPUT_KEYWORD` | 输入搜索关键词 | `com.xunmeng.pinduoduo:id/search_input_et` |
| 3 | `STEP_CLICK_SEARCH_BTN` | 点击搜索按钮 | `com.xunmeng.pinduoduo:id/search_confirm_btn` |
| 4 | `STEP_WAIT_RESULT` | 等待商品列表加载 | 检测「¥」「拼团」「找相似」等特征 |

**关键特性：**
- 启动时检测 Activity 类型，已在搜索/结果页则跳过 STEP_CLICK_SEARCH_ENTRY
- 步骤3支持"输入完自动出结果"的版本，无需再点搜索按钮
- 支持 `ACTION_IME_ENTER`（输入法回车键）触发搜索
- 20秒超时保护

### 2.3 修改 AndroidManifest.xml

在 `MeituanAutoService` 之后新增 `PinduoduoAutoService` 注册：

```xml
<service
    android:name=".PinduoduoAutoService"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>
```

---

## 改动汇总表

| 文件 | 改动类型 |
|---|---|
| `lib/utils/voice_intent_handler.dart` | 新增 `PINDUODUO_SEARCH` / `PINDUODU` handler |
| `android/.../MainActivity.java` | 新增 `startPinduoduoSearch` 通道 + `cachePinduoduoKeywordAndLaunch()` |
| `android/.../PinduoduoAutoService.java` | 新建文件，4步状态机 |
| `android/.../AndroidManifest.xml` | 新增 `<service>` 注册 `PinduoduoAutoService` |

---

## 重要：节点 ID 抓取步骤

`PinduoduoAutoService.java` 中的以下常量是**推测值**，必须替换为真实值：

```java
private static final String ID_HOME_SEARCH_ENTRY = "com.xunmeng.pinduoduo:id/search_container";
private static final String ID_HOME_SEARCH_TXT   = "com.xunmeng.pinduoduo:id/search_hint_text";
private static final String ID_SEARCH_ET        = "com.xunmeng.pinduoduo:id/search_input_et";
private static final String ID_SEARCH_BTN        = "com.xunmeng.pinduoduo:id/search_confirm_btn";
```

**抓取步骤：**

1. 手机连接电脑，开启 USB 调试
2. 打开拼多多 App，进入首页（确保搜索入口可见）
3. 在 Android Studio 中：Tools → Layout Inspector → 选择拼多多进程
4. 依次操作并抓取以下页面的节点 ID：
   - 首页：找到搜索入口容器的 `resource-id`
   - 搜索页：找到搜索输入框和搜索按钮的 `resource-id`
5. 将 `PinduoduoAutoService.java` 中的 4 个常量替换为真实值
6. 重新编译安装 APK，在"无障碍"设置中启用"拼多多自动化"服务

---

# 微信内打开指定小程序（手机充值）

## 能否用 Flutter + 安卓原生拉起小程序？

可以。官方方式是接入 **微信开放平台 Android SDK**，调用 `WXLaunchMiniProgram`，其中：

- `userName`：小程序 **原始 ID**（`gh_` 开头，与公众平台「更多资料」里「账号原始 ID」一致）。
- 截图里的小程序 **AppID**（`wxad3150031786d672`）用于别的能力；**拉起小程序接口填的是 `gh_fefb88a96b0e`**。

本项目中已在 `MainActivity` 常量 `RECHARGE_MINI_PROGRAM_USER_NAME = "gh_fefb88a96b0e"` 写死话费充值小程序；语音意图 `WECHAT_PHONE_RECHARGE` 会走 `launchWeChatPhoneRechargeMiniProgram`。

## 你必须完成的开放平台配置（否则 sendReq 无效）

1. 打开 [微信开放平台](https://open.weixin.qq.com)，创建 **移动应用**（不是仅小程序）。
2. 填写本 App 的包名：`com.example.phone_java`，并配置 **应用签名**（与打包签名 MD5 一致，可用微信提供的签名工具生成）。
3. 审核通过后得到 **移动应用 AppID**（`wx` 开头），写入工程根目录下 `android/gradle.properties`：
   ```properties
   wechat.app.id=wx你的移动应用AppId
   ```
4. 重新编译安装；真机需已安装微信。

## 工程改动摘要

| 位置 | 说明 |
|------|------|
| `android/app/build.gradle.kts` | 依赖 `wechat-sdk-android`，`BuildConfig.WECHAT_APP_ID` |
| `android/gradle.properties` | 配置 `wechat.app.id` |
| `wxapi/WXEntryActivity.java` | 微信回调入口（包名固定为 `应用包名.wxapi.WXEntryActivity`） |
| `AndroidManifest.xml` | 注册 `WXEntryActivity`；`queries` 增加 `com.tencent.mm` |
| `MainActivity.java` | `launchWeChatMiniProgram` / `launchWeChatPhoneRechargeMiniProgram` |
| `voice_intent_handler.dart` | `WECHAT_PHONE_RECHARGE` → MethodChannel |
| 后端 `ActionEnum` + `ChatAssistant` | 识别「充话费」等说法 → `WECHAT_PHONE_RECHARGE` |

## 扩展：拉起别的小程序或指定页面

- 通用通道：`voice_intent` → `launchWeChatMiniProgram`，参数 `userName`（必填）、`path`（可选，如 `pages/index/index?foo=1`）、`miniprogramType`（可选，正式版默认）。
- 或改 `MainActivity` 中 `RECHARGE_MINI_PROGRAM_USER_NAME` / 增加新常量与新 MethodChannel。



